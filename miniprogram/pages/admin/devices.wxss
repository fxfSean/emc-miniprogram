.add { margin-bottom: 24rpx; }
.device-card { transition: opacity .15s ease; }
.device-card:active { opacity: .72; }
.name { min-width: 0; overflow: hidden; font-size: 31rpx; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }
.line { display: block; margin-top: 10rpx; }
